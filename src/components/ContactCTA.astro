---
import profile from '@/data/profile.yaml'; const asset=(p:string)=>`${import.meta.env.BASE_URL}${p.replace(/^\//,'')}`;
---
<section data-reveal><div class="cta"><div><h2>Open to research collaboration on AIoT, sensing platforms, and edge intelligence.</h2></div><div class="actions"><a class="button" href={`mailto:${profile.email}`}>Email Minh-Hoang</a><a class="button" href={asset('/contact/')}>Contact details</a></div></div></section>
